{
  "name": "autodarts-plus",
  "version": "1.2.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "yarn build:chrome && yarn build:firefox",
    "build:chrome": "vite build --mode chrome && node zip-script.cjs chrome",
    "build:firefox": "vite build --mode firefox && node firefox-security-fix.cjs && node zip-script.cjs firefox"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "@crxjs/vite-plugin": "^2.0.0-beta.23",
    "archiver": "^6.0.0"
  }
}
